---
- name: Install Redis
  ansible.builtin.dnf:
    name: "{{ redis_rocky }}"
    state: latest
#  tags: redis

- name: start redis
  ansible.builtin.systemd:
    name: redis
    enabled: true
    state: started

